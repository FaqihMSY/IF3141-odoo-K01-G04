<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_order_form_automated_sales_invoicing" model="ir.ui.view">
        <field name="name">sale.order.form.automated.sales.invoicing</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button
                    name="action_create_automatic_invoice"
                    type="object"
                    string="Create Invoice Automatically"
                    class="btn-primary"
                    invisible="invoice_status != 'to invoice' or state not in ('sale', 'done')"
                />
            </xpath>
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="auto_invoice_created" readonly="1"/>
                <field name="auto_invoice_date" readonly="1"/>
                <field name="auto_invoice_id" readonly="1"/>
            </xpath>
        </field>
    </record>
</odoo>
