<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_invoice_payment_monitoring" model="ir.ui.view">
        <field name="name">account.move.form.invoice.payment.monitoring</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button
                    name="action_open_register_invoice_payment"
                    type="object"
                    string="Register Payment"
                    class="btn-primary"
                    invisible="move_type != 'out_invoice' or state != 'posted' or payment_state == 'paid'"
                />
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button
                    name="action_view_invoice_payment_logs"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-money"
                    invisible="move_type != 'out_invoice'"
                >
                    <field name="invoice_payment_log_count" widget="statinfo" string="Payments"/>
                </button>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page
                    string="Payment Monitoring"
                    invisible="move_type != 'out_invoice'"
                >
                    <group>
                        <field name="invoice_payment_status" readonly="1"/>
                        <field name="amount_total" readonly="1"/>
                        <field name="amount_residual" readonly="1"/>
                        <field name="payment_state" readonly="1"/>
                    </group>
                    <field name="invoice_payment_log_ids" readonly="1">
                        <tree>
                            <field name="payment_date"/>
                            <field name="invoice_number"/>
                            <field name="partner_id"/>
                            <field name="amount"/>
                            <field name="journal_id"/>
                            <field name="status_after_payment"/>
                            <field name="payment_id"/>
                            <field name="note"/>
                        </tree>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <record id="view_invoice_payment_monitor_tree" model="ir.ui.view">
        <field name="name">account.move.tree.invoice.payment.monitor</field>
        <field name="model">account.move</field>
        <field name="arch" type="xml">
            <tree string="Invoice Payment Monitor" create="false">
                <field name="name" string="Invoice Number"/>
                <field name="invoice_date"/>
                <field name="invoice_date_due"/>
                <field name="partner_id"/>
                <field name="amount_total"/>
                <field name="amount_residual"/>
                <field name="invoice_payment_status"/>
                <field name="payment_state"/>
            </tree>
        </field>
    </record>

    <record id="action_invoice_payment_monitor" model="ir.actions.act_window">
        <field name="name">Invoice Payment Monitor</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_invoice_payment_monitor_tree"/>
        <field name="domain">[('move_type', '=', 'out_invoice'), ('state', '=', 'posted')]</field>
        <field name="context">{'search_default_posted': 1}</field>
    </record>

    <menuitem
        id="menu_invoice_payment_monitor"
        name="Invoice Payment Monitor"
        parent="account.menu_finance"
        action="action_invoice_payment_monitor"
        sequence="83"
    />
</odoo>
